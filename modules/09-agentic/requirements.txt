# "openai" backs Ollama's OpenAI-compatible endpoint (providers/ollama.py);
# "openrouter" backs providers/openrouter.py's dedicated OpenRouterProvider;
# "cerebras" backs providers/cerebras.py's dedicated CerebrasProvider.
# Adding a new provider that needs its own SDK (Anthropic, Gemini, ...)
# means adding that extra here too, e.g. pydantic-ai-slim[openai,anthropic].
pydantic-ai-slim[openai,openrouter,cerebras]>=2.43.0
